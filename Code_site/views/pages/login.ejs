<!DOCTYPE html>
<html lang="en">

<head>
    <%- include ('../partials/head') %>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <link href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet"> 
    <style>
        body {
            background-color: #F7F5F7;
            color: #FAFAFF;
            font-family: 'Poppins', sans-serif;
            font-weight: 400;
            font-style: normal;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;

        }

        h2 {
            color: #FAFAFF;
            font-weight: bold;
        }

        .welcome {
            text-align: left;
            margin-bottom: 1.25rem;
        }

        .container {
            width: 35rem;
            height: 25rem;
            margin: 50px auto;
            padding: 20px;
            padding-left: 2rem;
            padding-right: 2rem;
            border-radius: 8px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
            background-color: #4F3CC9;
            margin-top: -10rem;
        }

        .form-group {
            margin-bottom: 15px;
        }

        label {
            display: block;
            margin-bottom: 5px;
        }

        input[type="text"],
        input[type="password"],
        input[type="email"] {
            width: 100%;
            padding: 10px;
            border: 1px solid #9747FF;
            border-radius: 4px;
            background-color: #FAFAFF;
            color: #09150E;
        }

        .btn-primary {
            background-color: #9747FF;
            color: #FAFAFF;
            padding: 10px 15px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s;
            width: 100%;
            font-size: 16px;
        }

        .btn-primary:hover {
            background-color: #8000e0;
        }

        p {
            margin-top: 15px;
            text-align: center;
        }

        a {
            color: #CCFF00;
            text-decoration: none;
        }

        a:hover {
            color: #CCFF00;
            text-decoration: underline;
        }

        .menu-overlay {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.8);
            justify-content: center;
            align-items: center;
            z-index: 999;
        }

        .menu-overlay.active {
            display: flex;
        }

        .menu-container {
            background-color: #4F3CC9;
            padding: 20px;
            border-radius: 8px;
            width: 80%;
            max-width: 300px;
            text-align: center;
        }

        .menu-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }

        .menu-header img {
            height: 30px;
        }

        .menu-header .close-button {
            background: none;
            border: none;
            color: #FAFAFF;
            font-size: 24px;
            cursor: pointer;
        }

        .navbar-item {
            color: #FAFAFF;
            text-decoration: none;
            display: block;
            margin: 10px 0;
        }

        .navbar {
            width: 100%;
            background-color: #F7F5F7;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 10px 20px;
            position: fixed;
            top: 0;
            left: 0;
            z-index: 1000;
        }

        .menu-button {
            border: none;
            outline: none;
            color: #ffffff;
            cursor: pointer;
            margin-top: 1rem;
            margin-right: 10rem; /* Move the menu button more to the right */
            display: flex;
            align-items: center;
            font-size: 1.5rem;
            gap: 0.5rem;
            background-color: rgba(0, 0, 0, 0.3);
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
            padding: 10px 20px;
            transition: background-color 0.3s, color 0.3s;
            font-variation-settings: 'FILL' 0, 'wght' 400, 'GRAD' 0, 'opsz' 48;
        }

        .menu-button:hover {
            color: #4F3CC9; /* Change the text color on hover */
        }

        .menu-button:focus {
            outline: none; /* Ensure no outline on focus */
        }

        .navbar-brand {
            margin-left: 10rem;
        }

        .navbar-brand img {
            height: 4rem; /* Increase the height for a larger logo */
        }

        .forgot {
            margin-top: -1rem;
            margin-bottom: 1rem;
        }
    </style>
</head>

<body>

    <nav class="navbar">
        <div class="navbar-brand">
            <a href="/">
                <img src="../img/Logo.png" alt="Unistays Logo">
            </a>
        </div>
        <button class="menu-button" onclick="toggleMenu()">
            <span class="material-symbols-outlined" style="font-size: 2rem;">menu</span>
            Menu
        </button>
    </nav>

    <div id="menuOverlay" class="menu-overlay">
        <div class="menu-container">
            <div class="menu-header">
                <img src="../img/Logo.png" alt="Unistays Logo" class="overlay-logo">
                <button class="close-button" onclick="toggleMenu()">&times;</button>
            </div>
            <a href="#how-it-works" class="navbar-item">How it Works</a><hr>
            <a href="#pricing" class="navbar-item">Pricing</a><hr>
            <a href="#FAQ" class="navbar-item">FAQ</a><hr>
            <a href="/login" class="navbar-item">Log In</a><hr>
        </div>
    </div>

    <main>
        <div class="container">

            <h2 class="welcome" style="margin-top:1rem;">Welcome back!</h2>

            <!-- login.ejs -->

            <form action="/login" method="POST">
                <div class="mb-3">
                    <label for="email" class="form-label">Email</label>
                    <input type="email" class="form-control" id="email" name="email" required>
                </div>
                <div class="mb-3">
                    <label for="password" class="form-label">Password</label>
                    <input type="password" class="form-control" id="password" name="password" required>
                </div>
                <div class="forgot">
                    <a href="/forgot-password">Forgot password?</a>
                </div>
                <button type="submit" class="btn btn-primary">Login</button>
            </form>


            <p>Don't have an account? <a href="/register">Register now!</a></p>

        </div>
    </main>

    <%- include ('../partials/footer') %>

    <script>
        function toggleMenu() {
            var menuOverlay = document.getElementById("menuOverlay");
            menuOverlay.classList.toggle("active");
        }
        </script>
</body>

</html>
