<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register</title>
    <link rel="stylesheet" href="../css/popup.css">
    <script defer src="../js/popup.js"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/select2/4.0.13/css/select2.min.css" rel="stylesheet" />
    <script src="https://cdnjs.cloudflare.com/ajax/libs/select2/4.0.13/js/select2.min.js"></script>
    <script src="https://maps.googleapis.com/maps/api/js?key=YOUR_API_KEY&libraries=places"></script>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined" rel="stylesheet">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/leaflet/1.7.1/leaflet.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/leaflet/1.7.1/leaflet.css">
    <style>
        .input-container {
            position: relative;
            width: 100%;
        }
    
        #residence {
            width: 100%;
            border: 1px solid #d4d4d4;
            border-radius: 4px; /* Rounded corners when not focused */
            outline: none; /* Removes the default focus outline */
            transition: border-radius 0.2s; /* Smooth transition for the border-radius */
        }
    
        #residence:focus {
            border-radius: 4px 4px 0 0; /* Top corners rounded, bottom corners straight */
        }

        #study:focus {
            border-radius: 4px 4px 0 0; /* Top corners rounded, bottom corners straight */
        }
        
        .autocomplete-items {
            position: absolute;
            border: 1px solid #d4d4d4;
            border-top: none; /* Removes the top border to blend with the input */
            z-index: 99;
            top: calc(100% - 20px);
            left: 0;
            right: 0;
            background-color: white;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1); /* Optional: Adds shadow for better separation */
        }

        #study-container .autocomplete-items {
            top: calc(100%); /* Adjust this value separately */
        }
    
        .autocomplete-items div {
            padding: 10px;
            cursor: pointer;
            background-color: #fff;
            border-bottom: 1px solid #d4d4d4;
        }
    
        .autocomplete-items div:hover, .autocomplete-active {
            background-color: #9747FF; /* Highlight color when hovered or active */
            color: white;
        }
    
        .autocomplete-active {
            background-color: DodgerBlue !important;
            color: #ffffff;
        }
    </style>
</head>
<body>
    <button id="open-popup">Register</button>

    <div id="popup" class="popup hidden">
        <div class="popup-content">
            <div class="logo-container">
                <span class="step-indicator">Step 1/4</span>
                <img src="../img/Logo.png" alt="Unistays Logo" class="unistays-logo">
                <span id="close-popup" class="material-symbols-outlined close-popup">close</span>
            </div>
            <div class="registration-step">
                <div class="form-section">
                    <label for="residence" class="where-to">Where do you want to live?</label>
                    <div class="input-container" id="residence-container">
                        <span class="material-symbols-outlined search-icon">search</span>
                        <input type="text" id="residence" name="residence" placeholder="Search City">
                    </div>

                    <label for="study" class="where-to">Where do you study?</label>
                    <div class="input-container" id="study-container">
                        <span class="material-symbols-outlined search-icon">search</span>
                        <input type="text" id="study" name="study" placeholder="Search University" disabled>
                    </div>

                    <div class="checkbox-container">
                        <input type="checkbox" id="not-student" name="not-student">
                        <label for="not-student">I am not a student</label>
                    </div>

                    <label for="radius" class="max-radius">Max radius (km)</label>
                    <div id="radius-container">
                        <input type="range" id="radius" name="radius" min="1" max="50" value="10">
                        <span id="radius-value">10 km</span>
                    </div>

                    <button id="next-step">Next Step</button>
                </div>
                <div id="map" class="map"></div>
            </div>
        </div>
    </div>
</body>

<script>

</script>
    
    
    
    


</html>
